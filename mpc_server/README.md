# MPC (Multi-Party Computation) Server

This server enables secure multi-party computation where multiple parties can jointly compute a function over their inputs while keeping those inputs private.

## Components

- Secure communication layer
- Cryptographic protocols
- Sub-agent interface for coding tasks
- Input validation and privacy preservation

## Setup

```bash
npm install # Install dependencies
node server.js # Start the server
```